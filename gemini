基于您提供的详细且高度结构化的工程架构和执行流水线，您的计划已经非常完备。为了确保在多轮 AI 交互中能够精确、无误地获取符合 arXiv:2404.11373v3  论文规格的完整代码，我已对您的提示词（Prompt）序列进行了优化。

优化策略如下：

1. **明确文件输出要求**：在每个 Prompt 中指定需要 AI 生成的具体文件路径，确保代码直接契合您的目录结构。
2. **强化论文硬约束**：将论文中明确的关键参数（如超参数、物理常数、截断条件）直接作为约束条件嵌入 Prompt。
3. **确保接口对齐**：在前后轮次的 Prompt 中强调模块间的输入输出（I/O）张量维度必须对齐。

以下是优化后的五轮交互 Prompt 序列，供您在后续开发中逐一发送：

---

### 第一轮：基础设施与物理信号层（对应您的第1周里程碑）

**Prompt 1:**

> “请为我的LISA黑洞光谱学推断工程编写第一阶段的核心基础代码。请严格根据 arXiv:2404.11373v3  的物理设定实现。
> 
> 
> **任务 1：编写 `src/rd_sbi/waveforms/ringdown.py**`
> * 实现基于重叠阻尼正弦波的时域 Ringdown 波形生成器（方程 1, 2a, 2b ）。
> 
> 
> * 包含复数 QNM 频率 $\tilde{\omega}_{lmn}$ 的生成逻辑，并按要求预留偏差参数 $\alpha$ 的接口。
> * 实现方程 (5)  的自旋加权球面调和函数近似（${}_{-2}Y_{lm}$）。
> * 确保输出维度：采样率 `2048 Hz` ，截断时长 `0.1 s` ，单探测器输出 `204 bins` 。
> 
> 
> 
> 
> **任务 2：编写 `src/rd_sbi/noise/whitening.py**`
> * 实现时域白化算法：输入 PSD -> 逆傅里叶变换求自相关函数（ACF）-> 构建 Toeplitz 协方差矩阵 -> Cholesky 分解求下三角矩阵 $L$ -> 逆矩阵 $L^{-1}$ 乘以应变 $h$（公式 A1 ）。
> 
> 
> **任务 3：编写 `tests/test_whitening.py**`
> * 编写 `pytest` 单元测试，验证白化后的纯噪声序列是否通过正态性检验（如 Shapiro-Wilk test），以确保 $x_{white}(\theta) = h_{white}(\theta) + \mathcal{N}(0,1)$（公式 A2 ）的假设成立。
> 
> 
> 请直接输出这三个文件的完整 Python 代码，并使用面向对象或纯函数式编程风格，添加类型提示（Type Hints）。”

---

### 第二轮：深度学习架构与密度估计器（对应您的第2周里程碑前半部）

**Prompt 2:**

> “基于第一阶段的成果，请编写第二阶段的深度学习模型代码。请严格遵循论文的架构超参数设定。
> **任务 1：编写 `src/rd_sbi/inference/embedding_net.py**`
> * 实现特征嵌入网络。输入张量维度必须是 `408`（H1 和 L1 探测器拼接：$204 + 204$）。
> * 结构：全连接网络，包含 2 个隐藏层，每层 `150` 个单元，输出维度降至 `128`（即 `408 -> 150 -> 150 -> 128`）。
> 
> 
> **任务 2：编写 `src/rd_sbi/inference/tsnpe_model.py` (配置 NSF)**
> * 使用 `sbi` 库的 `neural_nets` 模块配置神经样条流（NSF）。
> * **硬性参数**：`num_transforms=5` , `hidden_features=150` , `num_blocks=2` , `num_bins=10` , `batch_norm=True` 。
> 
> 
> * 确保 NSF 接收的上下文（Context）输入维度与 `embedding_net` 的输出（`128`）严格对齐。
> 
> 
> 请输出这两个文件的完整代码。要求代码可直接在 PyTorch 框架下实例化，并包含验证 `(Batch, 408) -> (Batch, 128)` 维度变换的前向传播测试（以注释形式附在文件末尾）。”

---

### 第三轮：核心优化——重写 sbi 损失与截断序列训练（对应第2周后半部）

**Prompt 3:**

> “这是复现工作中最核心的第三阶段。原作者通过面向对象编程包裹了 `sbi` 的 SNPE 实现，以支持在每个 Epoch 动态注入噪声 。
> 
> 
> **任务 1：编写 `src/rd_sbi/inference/sbi_loss_patch.py**`
> * 继承 `sbi` 库中用于 SNPE 的后验/损失类（如 `sbi.inference.snpe.snpe_base.SNPE` 及其内部的 loss 评估逻辑）。
> * 重写 loss 方法：在每次前向传播计算 loss 之前，接收干净的白化波形批次 $h_{white}(\theta)$，并动态加上实时采样的标准正态噪声 $\mathcal{N}(0,1)$ 生成 $x_{white}(\theta)$。
> * 在此处实现输入特征的在线标准化（使其具有零均值和单位方差）。
> 
> 
> 
> 
> **任务 2：编写 `src/rd_sbi/inference/tsnpe_runner.py**`
> * 实现截断序列神经后验估计（TSNPE） 的外层训练循环。
> * **硬性训练参数**：第一轮模拟 `50k` 样本，后续轮次模拟 `100k` 样本 。`batch_size=512` ，学习率 `0.001` ，验证集比例 `0.1` 。
> * **硬性截断参数**：使用最高后验密度（HPD）拒绝机制，截断分位数 $\epsilon = 10^{-4}$ 。
> * **硬性停止条件**：当当前轮次截断体积超过上一轮次的 80% 时 (`stopping_ratio=0.8`) 停止训练 。在 $r > 1$ 轮次，必须舍弃第一轮的模拟数据 。
> 
> 
> 请输出这两个文件的完整 Python 代码，确保重写的 loss 方法兼容最新的 `sbi` 库版本，并能在 GPU 上高效运行。”

---

### 第四轮：配置注入实验与验证基线（对应您的第3周里程碑）

**Prompt 4:**

> “前置管线已搭建完毕，现在需要配置论文中的注入实验并准备与 MCMC 基线的对比。
> **任务 1：编写 `scripts/02_make_injections.py**`
> * 实现生成 `Kerr220`, `Kerr221`, `Kerr330` 注入信号的脚本。
> * **固定参数**：$M_f=67 M_{\odot}$ , $\chi_f=0.67$ , $\{\alpha,\delta,\psi\}=\{1.95,-1.27,0.82\}$ 。GPS 开始时间 `t_H1=1126259462.42323` 。
> * **Kerr220 特定参数**：$\mathcal{A}_{220}=5\times10^{-21}$ , $\phi_{220}=1.047$ , $\iota=\pi$ , SNR≈14 。
> * **Kerr221 特定参数**：$\mathcal{A}_{220}=8.92\times10^{-21}$ , $\mathcal{A}_{221}=9.81\times10^{-21}$ , $\phi_{220}=1.047$ , $\phi_{221}=4.19$ , $\iota=\pi$ , SNR≈14 。
> * **Kerr330 特定参数**：$\mathcal{A}_{220}=30\times10^{-21}$ , $\mathcal{A}_{330}=3\times10^{-21}$ , $\phi_{220}=1.047$ , $\phi_{330}=5.014$ , $\iota=\pi/4$ , SNR≈53 。
> * 定义均匀先验（Box-uniform priors），范围依据论文 Table II  设定（如 $M_f \in [20, 300]$, $\chi_f \in [0, 0.99]$）。
> 
> 
> 
> 
> **任务 2：编写 `src/rd_sbi/baseline/pyring_runner.py**`
> * 编写调用 `pyRing` (v2.3.0)  软件的封装脚本。
> * 配置 `cpnest` 采样器参数：`4096 live points` ，`4094 maximum Markov-chain steps` ，以确保产生约 20k 后验样本 。
> 
> 
> 请输出这两个文件。注入脚本需保存生成的数据段 $x_{obs}$ 供后续推断使用。”

---

### 第五轮：覆盖率测试与图表复现（对应您的第4、5周里程碑）

**Prompt 5:**

> “最后阶段，需要实现严格的统计验证与论文图表复现。
> **任务 1：编写 `src/rd_sbi/eval/coverage_test.py**`
> * 实现基于重要性采样（Importance Sampling）的贝叶斯覆盖率测试（Eq. B2 ）。
> * **关键逻辑**：由于使用 TSNPE，覆盖率测试必须从最终截断后的先验 $\tilde{p}(\theta)$ 中抽样注入参数 $\theta_*$ 。
> 
> 
> * 实现对 100 个模拟目录（每个包含 100 个事件）评估经验累积分布函数（c.d.f. $f(\gamma)$），并计算 Kolomogorov-Smirnov (KS) test 的 p-value 。
> 
> 
> 
> 
> **任务 2：编写 `scripts/06_make_figures.py**`
> * 提供生成类似原论文 Figure 1, 2, 3 和 Appendix Figure 6, 7  的绘图脚本。
> * 要求使用 `seaborn` 和 `matplotlib` ，支持将 `sbi` 提取的 20k 后验样本 与 `pyRing` 样本绘制重叠的 Corner Plot（68% 和 90% 置信区间 ）以及振幅参数 $\mathcal{A}_{lmn}$ 的小提琴图（减去真值进行对齐 ）。
> 
> 
> 请提供这两个模块的完整代码。绘图脚本需能够直接读取保存的 posterior CSV/HDF5 文件并输出 PDF 格式图表。”